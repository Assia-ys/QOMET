[build]
command = "pip install -r requirements.txt && cd frontend/src && npm install && npm run build && cd ../.."

[deploy]
startCommand = "sh -c 'uvicorn backend.main:socket_app --host 0.0.0.0 --port $PORT'"
